apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth
  labels:
    app: auth
spec:
  selector:
    matchLabels:
      app: auth
  replicas: 2
  template:
    metadata:
      labels:
        app: "auth"
    spec:
      containers:
        - name: auth
          image: "pedronx/auth"
          resources:
            requests:
              memory: 20Mi
              cpu: "0.3"
            limits:
              memory: 25Mi
              cpu: "0.3"
          startupProbe:
            httpGet:
              path: /healthz
              port: 3333
            failureThreshold: 1
            periodSeconds: 3
            initialDelaySeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3333
            failureThreshold: 1
            periodSeconds: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3333
            failureThreshold: 1
            timeoutSeconds: 1
            successThreshold: 1
            periodSeconds: 5
          envFrom:
            - configMapRef:
                name: auth-env